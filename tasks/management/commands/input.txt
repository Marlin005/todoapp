помыть посуду
прочитать книгу
выбросить мусор

#обычная форма
class TaskListView(LoginRequiredMixin, ListView):
    #  убрали из определения класса queryset и просто указали модель.
    model = TodoItem
    context_object_name = "tasks"
    template_name = "task/list.html"
# #переопределили метод get_queryset:
#     def get_queryset(self):
# #первым делом мы получаем в переменную qs то-что-было-бы-получено-как-результат-работы-этого-метода- если-бы-мы-его-не-перегружали.
#         qs = super().get_queryset()
# #берём пользователя из запроса, который к нам пришёл
#         u = self.request.user
#         if u.is_anonymous:
# #Если же пользователь залогинен не был, то мы возвращаем пустой список
#             return []
# # возвращаем в шаблон уже только те таски, которые удовлетворяют условию фильтрации.
#         return qs.filter(owner=u)

# можно сделать все было по другому отнаследовав  свой обработчик дополнительно от класса LoginRequiredMixin
    def get_queryset(self):
        u = self.request.user
# так как теперь мы уверены, что мы находимся внутри защищённого логином окружения, мы можем убрать проверку на анонимность пользователя.
        return u.tasks.all()
